name: cognios
description: AI cognitive operating system — CogniOS
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_sign_in: ^6.2.1
  http: ^1.2.1
  flutter_markdown: ^0.6.22
  shared_preferences: ^2.2.3
  intl: ^0.19.0
  shimmer: ^3.0.0
  flutter_svg: ^2.0.10+1
  file_picker: ^8.0.0+1
  firebase_core: ^3.1.0
  firebase_messaging: ^15.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
